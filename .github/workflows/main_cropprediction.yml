name: Build and deploy CropPrediction ML models to DigitalOcean Droplet

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Zip artifact for deployment
        run: |
          zip -r release.zip Models/ Predict/ predict.py requirements.txt

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: crop-prediction-app
          path: release.zip
          if-no-files-found: warn

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: crop-prediction-app

      - name: Unzip artifact for deployment
        run: unzip release.zip

      - name: Deploy to DigitalOcean
        uses: appleboy/scp-action@v0.1.5
        with:
          host: 159.65.158.161
          username: root
          password: ${{ secrets.DROPLET_PASSWORD }}
          source: "Models/,Predict/,predict.py,requirements.txt"
          target: "/root/crop-prediction/"

      - name: Start prediction service
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: 159.65.158.161
          username: root
          password: ${{ secrets.DROPLET_PASSWORD }}
          script: |
            cd /root/crop-prediction

            # Create and activate virtual environment
            python3 -m venv venv
            source venv/bin/activate

            # Install dependencies inside venv
            pip install -r requirements.txt

            # Kill old process if running
            pkill -f 'python predict.py' || true

            # Run prediction script with nohup
            nohup venv/bin/python predict.py > app.log 2>&1 &
